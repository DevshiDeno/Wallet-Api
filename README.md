## Wallet API

Simple wallet transaction API built with Node.js and SQLite.

## Setup

npm install

## Run

npm run dev

Server runs on:
http://localhost:3000

## Endpoints

POST /accounts
POST /transactions/deposit
POST /transactions/transfer
GET /accounts/:id
